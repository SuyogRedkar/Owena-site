import {Resend} from 'resend';
export async function sendEmail(opts:{subject:string;html:string;attachments?:{filename:string;content:Buffer}[]}){const key=process.env.RESEND_API_KEY;if(!key)throw new Error('Email service is not configured. Add RESEND_API_KEY to your environment variables.');const from=process.env.FROM_EMAIL;if(!from)throw new Error('FROM_EMAIL is not configured.');const to=process.env.TO_EMAIL;if(!to)throw new Error('TO_EMAIL is not configured.');const resend=new Resend(key);const {error}=await resend.emails.send({from,to,subject:opts.subject,html:opts.html,attachments:opts.attachments});if(error)throw new Error(error.message)}
